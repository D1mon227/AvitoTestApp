import UIKit

enum Resources {
    enum Button {
        
    }
    
    enum Network {
        static let baseURL = "https://www.avito.st/s/interns-ios"
        
        enum Paths {
            static let mainPage = "/main-page.json"
            static let detailsPage = "/details/1.json"
        }
    }
}
